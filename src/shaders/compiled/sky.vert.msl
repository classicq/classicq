#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct type_UBO
{
    float4x4 mvp;
    float4 origin_speed1;
    float4 speed2;
};

struct main0_out
{
    float4 out_var_TEXCOORD0 [[user(locn0)]];
    float2 out_var_TEXCOORD1 [[user(locn1)]];
    float2 out_var_TEXCOORD2 [[user(locn2)]];
    float4 gl_Position [[position]];
};

struct main0_in
{
    float3 in_var_TEXCOORD0 [[attribute(0)]];
    float4 in_var_TEXCOORD3 [[attribute(3)]];
};

vertex main0_out main0(main0_in in [[stage_in]], constant type_UBO& UBO [[buffer(0)]])
{
    main0_out out = {};
    float3 _41 = in.in_var_TEXCOORD0 - UBO.origin_speed1.xyz;
    float3 _44 = _41;
    _44.z = _41.z * 3.0;
    float _46 = 378.0 / length(_44);
    float _48 = _41.x * _46;
    float _50 = _41.y * _46;
    out.out_var_TEXCOORD0 = in.in_var_TEXCOORD3;
    out.out_var_TEXCOORD1 = float2((UBO.origin_speed1.w + _48) * 0.0078125, (UBO.origin_speed1.w + _50) * 0.0078125);
    out.out_var_TEXCOORD2 = float2((UBO.speed2.x + _48) * 0.0078125, (UBO.speed2.x + _50) * 0.0078125);
    out.gl_Position = UBO.mvp * float4(in.in_var_TEXCOORD0, 1.0);
    return out;
}

